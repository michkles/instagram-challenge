<% if @pictures.any? %>
  <% @pictures.each do |picture| %>
    <%= link_to image_tag(picture.image.url), picture_path(picture) %><br>
  <% end %>
<% else %>
    <p>No pictures</p>
<% end %>


<h3><a href='/pictures/new'>Add a picture</a></h3>
